# Skip Playwright browser installation during npm install
# Browsers are only needed for local E2E testing, not for builds
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1

# Use legacy peer deps to allow React 19 with react-simple-maps
legacy-peer-deps=true
